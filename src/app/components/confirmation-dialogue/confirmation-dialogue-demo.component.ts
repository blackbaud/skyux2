import { Component } from '@angular/core';

@Component({
    selector: 'sky-modal-demo',
    templateUrl: './confirmation-dialogue-demo.component.html'
})
export class SkyConfirmationDialogueDemoComponent {
    public openModal(){
        /*
            figure out what providers are. put that.... in demo or main??
            filters uses modal pop up so base off of that
            figure out how filters works
            emitting for confirm and cancel: necessary?
                maybe not for cancel, in filters there is a closed thing afterwards, figure that out
                */0
    }
}