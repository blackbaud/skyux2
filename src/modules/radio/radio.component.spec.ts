import {
  TestBed
} from '@angular/core/testing';

import {
  SkyRadioModule
} from './radio.module';

describe('Radio component', function () {
  beforeEach(function () {
    TestBed.configureTestingModule({
      imports: [
        SkyRadioModule
      ]
    })
  });

  it('should update the ngModel properly when radio button is changed', function () {

  });

  it('should update the radio buttons properly when ngModel is changed', function () {

  });

  it('should pass a label when specified', function () {

  });

  it('should pass a labelled by id properly when specified', function () {

  });

  it('should pass a tabindex when specified', function () {

  });

  it('should set touched and pristine on blur', function () {

  });

});
